# encoding: utf-8

【DAKE正式出荷ライン｜2026-06-12 現行】

## 基本定義

DAKEシリーズでは、GitHub Release公開だけでは正式出荷完了としない。

正式出荷とは、正本から派生ビューが生成または整合され、必要な出荷先へ届いている状態である。

## 正式出荷ライン

```text
ORIGINAL.md
↓
README.md
↓
DAKE_META
↓
release_body.md
↓
assets/screenshot.webp
↓
assets/booth_thumbnail.jpg
↓
booth_product.txt / booth_ready
↓
build
↓
dist/*.exe
↓
GitHub Release
↓
BOOTH
↓
dakeapp.com
↓
store_products.generated.json
↓
dake-store-site同期
↓
store.dakeapp.com商品詳細確認
↓
payment_status確認
↓
Cloudflare反映確認
↓
正式出荷完了
```

## Storeの扱い

store.dakeapp.com は正本ではない。

販売ビューである。

ただし、正式出荷先に含める。

Store表示は ORIGINAL.md 由来の generated JSON から作る。

## CLOSED条件

次が揃うまで正式出荷完了として扱わない。

- ORIGINAL.md 確認
- README / DAKE_META / release_body / booth_product 整合確認
- screenshot.webp
- booth_thumbnail.jpg
- booth_ready
- dist/*.exe
- GitHub Release
- BOOTH導線
- dakeapp.com掲載
- store_products.generated.json再生成
- dake-store-site同期
- store.dakeapp.com商品詳細確認
- payment_status確認
- Cloudflare反映確認

## Store同期

正式出荷時は次を実行する。

```powershell
python tools\store\sync_store_to_site.py
```

同期後に確認する。

- items件数
- payment_status件数
- stripe_ready
- booth_only
- preparing
- not_for_sale
- source_policy
- do_not_edit: true
- shimarisu_pack

## 禁止

- GitHub Releaseだけで完了扱いする
- BOOTHだけで完了扱いする
- Storeだけで完了扱いする
- generated JSONを手編集する
- Store側で商品情報を直接編集する
- Stripe Secret / APIキー / Webhook Secretを公開repoへ入れる

## SNS投稿と予約設定（v2補足）

通常出荷ではSNS予約を行わず、Buffer下書き作成までを標準とする。

通常出荷のSNS工程は、媒体別投稿文生成、指定媒体のBuffer下書き作成、終了とする。
この工程では即時公開、予約公開、Queue投入を行わない。

予約設定は、ユーザーが対象アプリ・媒体・日時を明示し、予約計画を確認した後に限り実行する。

媒体はX・Threads・Instagramから任意に選択できる。
指定がない場合はX・Threads・Instagramの3媒体を標準対象とする。

Codexは通常出荷時に即時公開または自動予約を行わない。

v2_closedは、出荷証跡とBuffer下書きが揃い、人間が投稿・予約・修正・見送りを判断できる状態を表す。
予約済み・公開済みであることはv2_closedの必須条件にしない。

## Pack Stripe release command（Phase 16C追記）

Pack商品も単品正式出荷コマンドを使用する。

```powershell
python tools\release_product.py <product_id>
```

Packでは、Stripe payload生成前に以下を必須確認する。

- Pack ZIPが存在する。
- Pack ZIPのサイズとSHA256が正本・manifestと一致する。
- 購入後配布方式が `manual_email_private_download` として正本化されている。
- `purchase_delivery_ready=yes` である。
- 配布期限、購入者向け案内、再送、不達対応、共通手動配布ルール参照がある。

購入後導線が未確定のPackは、Stripe決済を開かない。

Pack ZIPや非公開配布URLをpublic repo、generated JSON、Store静的ファイルへ置かない。

Payment Link作成後は、確定したPayment Link URLだけを別工程で `ORIGINAL.md` へ書き戻す。
